```
노출할 page group 목록 수급 필요.
```
 
```
top / bottom countries
```

```
akamai 대시보드에서 나오는 차트와는 다르게 각 branch 별 TTVR
```

- ```
    country 기준으로 하는 경우
    ```
    
    ```
    sortby 옵션을 time_to_visually_ready로 주면 bottom은 구할 수 있지만,  top은 구할 수 없음 (sortby 옵션을 주면 내림차순 정렬되고, 오름차순으로는 정렬 불가)
    ```
    
    ```
    대안으로 최대 사이즈인 100개의 목록을 구한 뒤 ttvr 값으로 정렬해서 상위 10개를 출력
    ```
    
 
```
지금 출력되고 있는 정보는 현재 선택된 브랜치에 접속하는 각 국가별 TTVR 값이기 때문에 국가 - branch 매핑 목록을 적용할 수는 없음.
```

-