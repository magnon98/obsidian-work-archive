- ```
    이슈사항
    ```
    
    - ```
        시계열 데이터가 아님. 
        ```
        
        ```
        주기적으로 데이터를 수집/적재해야 시계열로 보여줄 수 있음.
        ```
        
          
        
    - ```
        지역(MENA, EU 등)에 대한 통합조회 방법
        ```
        
        ```
        개별 조회 후 재집계해야 하는 것인지…
        ```
        
        - ```
            각 지역에 소속된 국가는 어떻게 알 수 있는지? 
            ```
            
            ```
            특정한 속성? 하드코딩?
            ```
            
   

- ```
    Compliance Rate
    ```
    
    ```
    80개 국가를 개별조회하는 것은 비효율적. 
    ```
    
    ```
    다른 다른 방법 없는지 확인 필요.
    ```
    
      
    
- ```
    API 필터가 작동하지 않음.
    ```
    
    - ```
        한 페이지 개수가 기본 20개, 최대 100개로 제한되어 있어 filter 를 사용하지 못하면 active rule 확인 등을 위해 한 국가당 여러 번 조회 필요.
        ```
        
        ```
        필터가 사용 가능하다면 meta.pagination.total_pages 값으로 개수 확인 가능
        ```
        
      
    
- ```
    Rate Limit 확인 필요.
    ```
    
    -   
        
      

```
===============================================================
```
 
- ```
    Launch 에서 Property가 samsung.com 의 한 국가 (또는 지역)을 의미하는 것으로 보임.
    ```
    
    ```
    여러 국가(MENA, EU, All )에 대한 통합 조회는 어떻게 할 수 있는지.
    ```
    
      
    
- ```
    Adobe Launch를 통해 active된 태그 총 개수 및 week 기준 시계열 증감 선그래프
    ```
    
    - ```
        Rule 목록 조회 후 Active인 것을 필터링하면 국가별 개수 확인 가능
        ```
        
        ```
        data[].attributes.enabled == true
        ```
        
    
    ```
    Launch API는 현 시점의 데이터만을 보여주고 있어 ￼시계열 데이터는 별도로 수집해 두어야 함.
    ```
    
      
    
- ```
    PageLoad type 룰의 개수
    ```
    
    ```
    rule component의 Delegate Descriptor ID 의 값이 PageLoad type 중 하나인지 체크하는 것으로 개수 확인 가능
    ```
    
    ```
    마찬가지로 시계열 차트를 위해서는 데이터를 별도로 수집해둘 필요가 있음.
    ```
    
      
    
- ```
    Compliance Rate
    ```
    
    ```
    국가별 PageLoad type 룰의 개수를 조회하면 가능하나, 
    ```
    
    ```
    80개 국가를 개별 조회하는 것은 비효율
    ```
    
    ```
    다른 방법이 없는지 확인 필요.
    ```
    
      
    
- ```
    태그벤더
    ```
    
    ```
    Extension 기능 파악 필요.
    ```