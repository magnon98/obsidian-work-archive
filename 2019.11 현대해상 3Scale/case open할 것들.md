1. ```
    APICAST_PATH_ROUTING 을 활성화하고 Public base URL이 동일한 API를 하나 더 만들면 zync가 생성하는 route 가 에러.
    ```
    
    ```
    에러가 발생하지 않도록 할 방법
    ```
    
    ```
    zync 가 route를 자동 생성하는 것을 막을 방법
    ```
     
```
인증서 등록 후 API service 업데이트하면 인증서 안지워지나?
```
 
```
서로 다른 클러스터에 설치된 2개의 3scale 중 한 쪽에서 다른 쪽으로 api service / applicatoin plan / application 등을 migration 하는 방법이 있는가? (DB 백업 말고 설정을 export / import )
```
       
```
 
```