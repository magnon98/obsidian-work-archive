- ```
    openapi 사업 때 OCP 를 3년으로 계약해서 EoS 에 별다른 영향 받지 않음
    ```
    
    ```
    OCP 3.11 + 3Scale 2.6 유지
    ```
    
- ```
    migration tool의 문제점
    ```
    
    - ```
        promote를 수동으로 진행하고 있고, 변경사항이 없어도 promote 버튼이 활성화되고 있음
        ```
        
        ```
        어떤 API를 promote 해야 하는지 알기 어려움
        ```
        
- ```
    hot deploy 이슈는 WAS 재시작 없이 class 파일 변경/적용 하는 것을 의미
    ```
    
    ```
    WAS 설정의 문제인데 일부 class만 hot deploy되지 않고 있고,
    ```
    
    ```
    hot deploy 관련 설정이 명확히 정리되지 않음
    ```
    
- ```
    커스텀 포탈 개발 필요
    ```
    
    ```
    원화 표기
    ```
    
    ```
    인보이스 발행
    ```
    
    ```
    access log 확인
    ```
    
    ```
    한글화
    ```
    
    ```
    migration ( account, application 등 포함)
    ```
    

```
헬스케어 서버 사용중이던 middle ware는 분리할 예정
```

- ```
    제휴사 중 대형 업체(리치앤코 등)에서 초단위로 크롤링하는 경우가 있음. 제한할 방법이 있는지?
    ```
    
    ```
    application plan 에서 quota 설정 방법 안내함
    ```
    
- ```
    기간계 작업 등으로 서비스를 일시 중지시킬 방법 있나?
    ```
    
    ```
    application의 suspend 방법 가이드
    ```