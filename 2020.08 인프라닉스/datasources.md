- ```
    KT IaaS
    ```
    
    ```
    aaa
    ```
    
      
    
- ```
    NHN IaaS
    ```
    
    ```
    aaa
    ```
    
      
    
- ```
    Openstack IaaS
    ```
    
    ```
    aaa
    ```
    
       
- ```
    PaaS-TA Hypervisor
    ```
    
    ```
    resource: Openstack API
    ```
    
      
    
- ```
    PaaS-TA Bosh
    ```
    
    ```
    InfluxDB - bosh_metric_db
    ```
    
      
    
- ```
    PaaS-TA VM
    ```
    
    ```
    MySQL - vms, zones
    ```
    
    ```
    InfluxDB - cf_metric_db
    ```
    
      
    
- ```
    PaaS-TA CF
    ```
    
    ```
    MariaDB - vms, zones, alarm_policies
    ```
    
    ```
    InfluxDB - cf_metric_db
    ```
    
      
    
- ```
    PaaS-TA CaaS (k8s)
    ```
    
    ```
    MariaDB - batch_alarm_executions, batch_alarm_infos, batch_alarm_receivers, batch_alarm_sns
    ```
    
    ```
    Prometheus
    ```
    
      
    
- ```
    PaaS-TA SaaS
    ```
    
    ```
    aaa
    ```
    
      
    
- ```
    rancher-k8s
    ```
    
    ```
    a
    ```