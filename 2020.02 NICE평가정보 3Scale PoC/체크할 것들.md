- ```
    메모리를 8 / 8 / 12 로 사용 가능한지
    ```
    
    - ```
        제공 예정 VM
        ```
        
        ```
        2 vCPU, 4 GB, 50 GB
        ```
        
        ```
        2 vCPU, 8 GB, 50 GB
        ```
        
        ```
        4 vCPU, 16 GB, 100 GB
        ```
        
          
        
- ```
    필수 지원 브라우저에 IE9 포함
    ```
    
    ```
    IE11 은 작년까지 공식 지원하였으나, 2020. 01. 01 부터 deprecated
    ```
    
   

- ```
    오프라인 설치인지?
    ```
    
    ```
    'PoC 검증을 위해 필요한 설치파일 등' 을 사전에 이메일로 전송하도록 되어 있음.
    ```
    
    ```
    RHEL 이미지까지 필요?
    ```
    
 
```
와일드카드 인증서는 제공 되는 것?
```
 
- ```
    DNS 설정은 hosts 파일을 이용?
    ```
    
    ```
    DNS 서버 지원은 없는건지?
    ```
    
    ```
    별도 구성해도 되나? 되겠지?
    ```
    
 
- ```
    레거시의 형태는?
    ```
    
    ```
    http 가 아닌 경우 Fuse 필요해짐
    ```