- apicast logging policy 일부 수정 ( {{host}} --\> {{original_request.host}} )
- oracle DB연동 관련 오픈한 케이스 응대
- apicast의 컨테이터로그를 파일로 저장할 방법 확인 (from 권은동 차장)
    - env에 APICAST_ACCESS_LOG_FILE 추가하고 컨테이너 내의 로컬 경로를 지정하여 파일로 저장하는 것이 가능하지만
    - scaleout이 가능해야 하는 pod이기 때문에 network storage나 host_path 등에 저장하는 경우 write lock이 걸려 경합 발생 가능성 있음.
    - 일단은 apicast는 그대로 두고 EFK에서 중복 수집되는 것을 해결하는 방향으로 결정