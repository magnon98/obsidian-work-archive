```
오류 메시지 변환 테스트 지원 (from 남기덕 과장, 전진호 차장)  
    - scope 에러 메시지가 통일되어 있지 않아 invalid scope 로 모두 변경  
    - card product의 point API에 limit 설정  
        - card-default plan에서 '포인트 정보 조회' Method에 분당 5회 설정  
        - 응답 메시지가 'Usage Limit Exceeded'인데 응답 메시지 정리한 문서에는 'Too Many Requests'로 되어 있어 각 프로덕트에서 limit 관련 response body를 'Too Many Requests'로 변경  
    - card product에 ip whitelist policy 추가  
          - 오류 메시지 확인을 위해 123.123.123.123 IP만 허용하고 나머지 모두 거부하도록 설정  
    - gateway timeout 테스트를 위해 timeout 설정 추가  
        - configmap/gateway-timeout 생성하여 dc/apicast-production에 mount
```
 
```
    - 테스트를 위해 설정한 내용은 gateway timeout 설정 제외하고 모두 원복
```
   

```
API의 route에 zync용 annotation 및 label이 붙어 있어 제거
```